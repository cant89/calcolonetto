export const getResult = ({ VAT, VAT_TYPE, VAT_YEAR, PENSION, SALARY }) => {
  // const vat = {
  //   type: VAT_TYPE || getVatType({ SALARY })
  //   coeff:
  // }
  /*
  {
    net,
    vat: {
      type,
      coeff,
      fivePercent
    }
    taxes: {
      irpef: {
        amount,
        percentage
      }
      regional: {
        amount,
        percentage
      },
      comunal: {
        amount,
        percentage
      },
    },
    pension: {
      amount,
      percentage,
      type
    },


  }
  */
};
